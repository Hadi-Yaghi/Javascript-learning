/*  variable Intro
    -what is a variable
    -why we use variable
    -Declare A variable and use
    -Syntax(keyword | variable name | Assignement operator )
    -variable withoot Var 
    =Multiple variable in the same line
    -Id and global Variable 
    -loosely typed vs strongly typed
*/
var user = hadi;

console.log(user);

hello.innerHtml = "hello";
